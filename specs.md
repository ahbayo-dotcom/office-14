# مواصفات مشروع مكتب 14 (البحث الذكي)

## البنية المعمارية (Architecture)
المشروع مبني على تقنيات الويب الأساسية (HTML, CSS, JS) ويعمل بالكامل على جانب العميل (Client-Side) باستخدام مكتبة `SheetJS`.
تم تحويل المشروع من "الرفع اليدوي" إلى "الجلب التلقائي" ليقرأ البيانات من مجلد مخصص.

## المكونات الأساسية
1. **ملفات الواجهة:** `index.html`, `styles.css`, `script.js`
2. **مجلد البيانات (Data):** يحتوي على ملفات الإكسل بصيغة `.xlsx`.
3. **ملف الإعدادات (config.json):** ملف يقرأه البرنامج لمعرفة أسماء ملفات الإكسل المتاحة لتحميلها.
4. **سكربت المزامنة:** ملف `.bat` مخصص لتسهيل عملية رفع التحديثات إلى GitHub عبر أسطر الأوامر (Git CLI).

## آلية العمل (Workflow)
1. يقوم المدير بتعديل ملفات الإكسل داخل مجلد `Data`.
2. يتم تشغيل ملف السكربت لرفع التعديلات إلى المستودع السحابي.
3. يفتح الموظف الرابط، فيقوم البرنامج بقراءة `config.json` وجلب أول ملف إكسل تلقائياً، مع توفير قائمة منسدلة لاختيار ملفات أخرى.
